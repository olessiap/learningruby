class Node
  def initialize(value)
    #takes in next value, a data
    #consturctor with value
    @value = value
    @next = next
    #@memory = Node.new(value)
  end


    def prepend(value)
      #changes the head of the list
      #creae new head value
      #new head's next value links to the old head
      #change the head pointer
    end
    
end
